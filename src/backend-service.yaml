apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend  # Debe coincidir con el label de tu Deployment
  ports:
    - protocol: TCP
      port: 80      # Puerto externo
      targetPort: 8000 # Puerto de FastAPI
  type: LoadBalancer